*.pdf filter=lfs diff=lfs merge=lfs -text
*.docx filter=lfs diff=lfs merge=lfs -text

# Flutter web build output (Dart compiled to JS so the teacher portal runs in
# a browser). Mark as generated so GitHub's Linguist excludes it from the
# language pie chart — without this the repo displays as ~73% JavaScript.
Manhaji/backend/src/main/resources/static/app/** linguist-generated=true
*.dart.js linguist-generated=true
